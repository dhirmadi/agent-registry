# ── Agentic Registry — Environment Variables ──────────────────

# Required
DATABASE_URL=postgres://registry:localdev@localhost:5432/agentic_registry?sslmode=disable
SESSION_SECRET=                              # generate with: openssl rand -hex 32
CREDENTIAL_ENCRYPTION_KEY=                   # generate with: openssl rand -base64 32

# Server
PORT=8090                                    # HTTP listen port
LOG_LEVEL=info                               # debug | info | warn | error
EXTERNAL_URL=http://localhost:8090           # Public URL (used for OAuth redirect_uri)

# Google OAuth (optional — omit to hide Google login button)
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=

# OpenTelemetry (optional — omit endpoint to disable export)
# OTEL_EXPORTER_OTLP_ENDPOINT=localhost:4317
OTEL_SERVICE_NAME=agentic-registry
# OTEL_TRACES_ENABLED=true
# OTEL_METRICS_ENABLED=true
# OTEL_LOGS_ENABLED=true

# Webhooks
WEBHOOK_TIMEOUT=5                            # Delivery timeout (seconds)
WEBHOOK_RETRIES=3                            # Retry count on failure
WEBHOOK_WORKERS=4                            # Concurrent delivery goroutines
